import React from "react";

const StepPhoneEmail = (props: any) => {
  return (
    <>
      <div>StepPhoneEmail</div>
      <button onClick={props.onClick}>Next</button>;
    </>
  );
};

export default StepPhoneEmail;
