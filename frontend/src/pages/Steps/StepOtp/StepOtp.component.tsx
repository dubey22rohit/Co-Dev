import React from "react";

const StepOtp = (props: any) => {
  return (
    <>
      <div>STEP OTP</div>
      <button onClick={props.onClick}>Next</button>;
    </>
  );
};

export default StepOtp;
