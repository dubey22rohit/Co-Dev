import React from "react";

const StepUsername = (props: any) => {
  return (
    <>
      <div>StepUsername</div>
      <button onClick={props.onClick}>Next</button>;
    </>
  );
};

export default StepUsername;
